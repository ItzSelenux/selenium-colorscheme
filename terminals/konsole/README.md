# Selenium for Konsole
- Put the files into ```~/.local/share/konsole/```
- Change colorscheme
    -  ``` Menu Button -> Settings -> Configure Konsole... -> Profiles -> (your profile name) -> Edit -> Appearance -> Color scheme & font ```
