# Selenium for Alacritty
- Put `selenium.toml` in any location, like ```~/.config/alacritty/themes/```

- Edit your configuration file
    -  ```
    	[general]
		import = [
		"~/.config/alacritty/themes/selenium.toml"
		]
```
